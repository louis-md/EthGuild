import Card from '@material-ui/core/Card'
import styled from 'styled-components'

const Payments = () => <StyledCard>You have no payments yet.</StyledCard>

export default Payments

const StyledCard = styled(Card)`
  width: 800px;
`
